from django.test import SimpleTestCase


class LeaseSanityTests(SimpleTestCase):
    """Placeholder — full lease/invoice tests to be rewritten against the
    merged assets.Asset-backed model."""

    def test_placeholder(self):
        self.assertTrue(True)
